#include<bits/stdc++.h>
using namespace std;
int main()
{
    int a, b;
    int count = 1;
    cin >> a >> b;
    while(a <= b){
        a = 3 * a;
        b = 2 * b;
        if(a <= b){
            count++;p  
        }
    }
    cout << count << endl;
}